package main

import "testing"

func TestEncrypt(t *testing.T) {
	// Test encrypt init segment

	// Test encrypt media segment with pssh

	// Test encrypt combined init and media segment
	// Should have same init segment as above

	// Test encrypt media segment with separate init segment
	// Should be same as media segment as above

	// Should maybe also try decrypt and get back the same content

}
